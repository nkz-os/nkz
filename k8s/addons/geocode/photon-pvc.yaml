---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photon-index
  namespace: nekazari
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 30Gi  # tune to EU extract size (Open item)
