# =============================================================================
# TLS certificate for MinIO public S3 API (minio.YOUR_DOMAIN)
# =============================================================================
# Preconditions:
# - DNS A/AAAA record for minio.YOUR_DOMAIN points to the cluster ingress.
# - Apply after ClusterIssuer letsencrypt-prod exists.
# =============================================================================

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: minio-tls
  namespace: nekazari
spec:
  secretName: minio-tls
  duration: 2160h
  renewBefore: 360h
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: minio.YOUR_DOMAIN
  dnsNames:
    - minio.YOUR_DOMAIN
