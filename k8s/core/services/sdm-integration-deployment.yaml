# =============================================================================
# SDM Integration Service Deployment
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: sdm-integration
  namespace: nekazari
  labels:
    app: sdm-integration
    layer: apis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sdm-integration
  template:
    metadata:
      labels:
        app: sdm-integration
        layer: apis
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: sdm-integration
          image: ghcr.io/nkz-os/nkz/sdm-integration:latest
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 5000
          env:
            - name: ORION_URL
              value: "http://orion-ld-service:1026"
            - name: KEYCLOAK_URL
              value: "http://keycloak-service:8080"
            - name: MONGODB_URL
              valueFrom:
                secretKeyRef:
                  name: sdm-secrets
                  key: mongodb-url
            - name: LOG_LEVEL
              value: "DEBUG"
            - name: KEYCLOAK_PUBLIC_URL
              valueFrom:
                configMapKeyRef:
                  name: nekazari-config
                  key: KEYCLOAK_PUBLIC_URL
            - name: KEYCLOAK_HOSTNAME
              valueFrom:
                configMapKeyRef:
                  name: nekazari-config
                  key: KEYCLOAK_HOSTNAME
            - name: HMAC_SECRET
              valueFrom:
                secretKeyRef:
                  name: sdm-secrets
                  key: hmac-secret
            - name: ALLOW_JWT_FALLBACK
              value: "false"
            - name: REQUIRE_HMAC_SIGNATURE
              value: "false"
            # IoT Agent provisioning
            - name: IOT_AGENT_URL
              value: "http://iot-agent-json-service:4041"
            - name: MQTT_HOST
              value: "mosquitto-service"
            # External MQTT endpoint shown to users in credentials modal
            # Must resolve from external networks (devices connecting to the platform)
            - name: MQTT_EXTERNAL_HOST
              valueFrom:
                configMapKeyRef:
                  name: nekazari-config
                  key: MQTT_EXTERNAL_HOST
            - name: MQTT_EXTERNAL_PORT
              valueFrom:
                configMapKeyRef:
                  name: nekazari-config
                  key: MQTT_EXTERNAL_PORT
            # NGSI-LD context URL (cluster-internal)
            - name: CONTEXT_URL
              value: "http://api-gateway-service:5000/ngsi-ld-context.json"
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "100m"
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: sdm-integration-service
  namespace: nekazari
spec:
  selector:
    app: sdm-integration
  ports:
    - port: 5000
      targetPort: 5000
