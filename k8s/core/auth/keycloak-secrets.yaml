# =============================================================================
# Keycloak Secrets - NOTA: Este archivo NO debe aplicarse directamente
# Los secretos se crean automáticamente por el script deploy.sh
# Este archivo es solo para referencia
# =============================================================================

# IMPORTANTE: Los secretos de Keycloak se crean automáticamente por deploy.sh
# NO aplicar este archivo directamente - usar deploy.sh

# Los secretos que se crean son:
# - keycloak-secret (admin-username, admin-password)
# - postgresql-secret (password) - ya existe del deploy original
# - keycloak-client-secrets (frontend-secret, admin-secret, api-gateway-secret)

# Para usar Keycloak con contraseñas generadas por deploy.sh:
# 1. Ejecutar deploy.sh primero (crea postgresql-secret)
# 2. Luego aplicar solo los deployments de Keycloak
# 3. Los secretos se sincronizan automáticamente
