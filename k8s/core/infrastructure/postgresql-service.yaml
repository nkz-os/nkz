apiVersion: v1
kind: Service
metadata:
  name: postgresql-service
  namespace: nekazari
  labels:
    app: postgresql
    layer: database
spec:
  selector:
    app: postgresql
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  type: ClusterIP
