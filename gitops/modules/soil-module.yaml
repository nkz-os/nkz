# Soil module — source of truth: nkz-module-soil repo (GitOps strict).
# Syncs Deployment, Service from the module repo.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: soil-module
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/nkz-os/nkz-module-soil
    targetRevision: HEAD
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: nekazari
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
