[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    orion_integration: requires running Orion-LD instance
    pg_integration: requires running PostgreSQL instance
